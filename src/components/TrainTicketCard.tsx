import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const TrainTicketCard = () => {
  return (
    <View>
      <Text>TrainTicketCard</Text>
    </View>
  );
};

export default TrainTicketCard;

const styles = StyleSheet.create({});
